import { SignInClient } from '@/components/auth/SignInClient'

export const metadata = {
  title: 'Sign In — SolviqLab',
  robots: { index: false },
}

export default function SignInPage() {
  return <SignInClient />
}
