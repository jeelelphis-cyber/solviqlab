// Sprint C-1.7 — Notification Provider implementation — placeholder
// Implements: NotificationProvider interface (contracts/index.ts).
// Enforces anti-spam: max 2 push/day, max 1 email/week, cooling period post-L2+.
export {}
