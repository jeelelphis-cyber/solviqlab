// Sprint C-1.7 — Conversation Provider implementation — placeholder
// Implements: ConversationProvider interface (contracts/index.ts).
// Wraps AICompanion as the conversation core — no dialogue logic duplicated here.
// Free/Premium: template renderer. Pro: LLM-powered (quota-limited).
export {}
