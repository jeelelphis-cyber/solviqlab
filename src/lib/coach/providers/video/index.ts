// Sprint C-1.6 — Video Provider implementations — placeholder
// Implements: VideoProvider interface (contracts/index.ts).
// Providers: HeyGenProvider, SynthesiaProvider, D_IDProvider, TextFallbackProvider.
// HeyGen is the default; fallback activates on error or latency > 120s.
export {}
