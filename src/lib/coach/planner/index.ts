// Sprint C-1.4 — Coach Planner Adapter — placeholder
// Implements: CoachPlanner interface — dispatches 'coach:plan_adapt' to EventBus.
// Never calls PlannerEngine.adapt() directly (P-16).
export {}
