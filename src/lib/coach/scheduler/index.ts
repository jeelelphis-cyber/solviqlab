// Sprint C-1.5 — Coach Scheduler — placeholder
// Implements: Vercel Cron-based morning/evening video scheduling.
// Queries DB for users in preferred_morning_time window (15-min buckets).
// DB index on (preferred_morning_time, timezone) required before activation.
export {}
